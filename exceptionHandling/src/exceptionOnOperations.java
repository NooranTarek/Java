
public class exceptionOnOperations extends Exception{ //this is checked exception
public exceptionOnOperations(String exceptionMessage){
    // to call constructor of the superclass or class that i extend from like exception here :)
    // pass my message to the constructor
    super(exceptionMessage);
}
}
